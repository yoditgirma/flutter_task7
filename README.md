# Flutter Animated Card

A simple Flutter UI project demonstrating how to create an **interactive animated card** using `AnimatedContainer`.

The card changes its **size, border radius, and color** when the user presses the **Select button**, showcasing Flutter's built-in animation capabilities.

---
